const arr = [1, 2, 3, 4, 5];
arr.map(item => item + 10);
 return item + 10;

 //--------------------------------

const usuario = () => ({ nome: 'Diego', idade: 23 }, usuario.idade);

console.log(usuario());

//----------------------------------

const nome = "Diego";
const idade = 23;
const mostraUsuario = () => ({nome = 'Diego', idade = 18}) 
 
console.log(mostraUsuario());

//-----------------------------------

const promise = () => (new Promise(resolve, reject), resolve())
  
 console.log(promise);

 //-----------------------------------

 